// import { PostController } from '../../src/post.controller';
// import { PostService } from 'apps/post/src/post.service';

describe('PostController', () => {
    // let postController: PostController;
    // let postService : PostService;
    // beforeEach(() => {
    //     postService = new PostService();
    //     postController = new PostController(postService);
    // });

    // describe('getHello', () => {
    //   it('should return hello world',async () => {
    //     const result = 'Hello World!';
    //     jest.spyOn(postService, 'getHello').mockImplementation(() => result);

    //     expect(await postController.getHello()).toBe(result);
    //   })
    // })

    describe('test', () => {
      it('should check if 1 == 1 ', () => {
        expect(1).toEqual(1);
      })
      
    })
});
